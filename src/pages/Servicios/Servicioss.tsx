import './Servicios.scss';
import * as React from 'react';

interface IServicioProps {
}

const Servicio: React.FunctionComponent<IServicioProps> = (props) => {
  return (
    <div>Servicios</div>
  );
};

export default Servicio;
